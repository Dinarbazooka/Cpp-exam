Поиск всех пиков. В последовательности целых чисел найти номера и значения всех элементов, 
которые строго больше предыдущего и последующего элементов. 
Первое и последнее число не учитываются, т.к. у них только по одному соседу. 
В первой строке задаётся количество чисел в последовательности, а во второй - сами числа.

package main 

import "fmt"

func main() {
	fmt.Println("Введите количество членов последовательности ")
	var n int
	fmt.Scan(&n)
	fmt.Println("Введите первый член последовательности ")
	var n1 int
	fmt.Scan(&n1)
	fmt.Println("Введите второй член последовательности ")
	var n2 int
	fmt.Scan(&n2)
	for i := 2; i < n; i++ {
		fmt.Println("Введите следующий член последовательности ")
		var nn int
		fmt.Scan(&nn)
		if n2 > n1 && n2 > nn {
			fmt.Printf("Значение пика = %v, его номер, начиная с нуля, = %v \n", n2, i-1)
		}
		n1 = n2
		n2 = nn
	}
}
