Злой профессор
Профессор ведет занятия у группы из N студентов. 
Расстроенный их дисциплиной, он решил отменять занятие если к его началу подошло менее K студентов. 
Зная время прибытия студентов и их требуемое количество, нужно определить состоялось ли занятие.
Формат входных данных
Первая строка содержит число T - количество наборов данных для анализа.
Каждый набор состоит из двух строк. 
Первая строка содержит 2 целых числа, разделенных пробелом, N (всего студентов) и K (пороговое значение). 
Вторая строка содержит N целых чисел разделенных пробелом, описывающих время прибытия каждого студента.
Внимание: отрицательное число или 0 означают, что студент подошел до начала пары или ровно в момент ее начала; 
положительное число обозначает опоздание студента.
Формат выходных данных
Для каждого набора данных напечатать ДА, если занятие состоялось и НЕТ, в обратном случае.


package main 

import "fmt"

func main() {
	fmt.Println("Введите количество наборов данных ")
	var n int
	fmt.Scan(&n)
	for i := 0; i<n; i++ {
		fmt.Println("Введите общее количество студентов ")
		var N int
		fmt.Scan(&N)
		fmt.Println("Введите пороговое значение ")
		var porog int
		fmt.Scan(&porog)
		fmt.Println("Введите время прибытия каждого студента ")
		count := 0
		for j := 0; j<N; j++ {
			var asakura int
			fmt.Scan(&asakura)
			if asakura > 0 {
				count++
			}
		}
		if count < porog {
			fmt.Println("НЕТ")
		} else {
			fmt.Println("ДА")
		}
	}
}
